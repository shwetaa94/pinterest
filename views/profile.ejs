<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/stylesheets/profile.css">
   
        <title> Profile</title>
        <script>
            function editDescription() {
                // Assuming you have a way to prompt the user for a new description
                var newDescription = prompt("Enter a new description:");
        
                // Find the element by its ID
                var descriptionElement = document.getElementById("userDescription");
        
                // Update the text content with the new description
                if (descriptionElement && newDescription !== null) {
                    descriptionElement.textContent = newDescription;
                }
            }
        </script>
        
</head>
<body>
    <div class="card-container">
       
        <div class="left">
            <img class="dp" src="https://randomuser.me/api/portraits/women/79.jpg" alt="user" />
        <p class="usernamee" >@<%= user.username %></h2>
        </div>
        <div class="right">
            <button class="edit" onclick="editDescription()">edit</button>
            <p class="fullnamee"><%= user.fullname %></h1>
                
                <p class="description" id="userDescription">You have the power to accomplish everything you set out to do today.</p>

                <form action="/logout" >
                <button class="logoutbttn"> Logout</button>
                </form> 
                <form action="/upload" method="post" enctype="multipart/form-data">
                    <input type="text" name="filecaption" placeholder="Enter caption" >
                    <input type="file" name="file" >
                    <input type="submit" value="upload">
                </form>
        </div>
       
      
    </div>

  
    <div class="content">
        <% user.posts.forEach( function(post){   %>
            <% console.log(post.image) %>
            <div class="item">
                <img src="/images/uploads/<%=post.image %>" alt="no data">
                <h3><%= post.postText %></h3>
            </div>
   
            <% }) %>
            
        
        <div class="item" >
          <img src="https://source.unsplash.com/random/200x200?sig=191" />
        </div>
        <div class="item" >
          <img src="https://source.unsplash.com/random/200x200?sig=210" />
        </div>
        
    </div>

    
   
</body>
</html>
